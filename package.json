{
  "name": "mind-practice-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npx serve -s dist -l ${PORT:-3000}"
  },
  "dependencies": {
    "axios": "^1.15.2",
    "framer-motion": "^12.38.0",
    "lucide-react": "^1.8.0"
  }
}
